# qtop PR 357 Good-Run Evidence

These screenshots and text renders support the #357 meta-PR closeout. They are
current good-run evidence from the Slurm sample set on the `develop` baseline.

- `large_cluster`: 18 nodes x 16 cores = 288 cores
- `large_mixed`: 20 nodes x 16 cores = 320 cores
- `large_multi_partition`: 18 nodes x 16 cores = 288 cores

The images are copied from the validated Slurm render set used for #415 because
#357 is documentation-only and does not change qtop rendering behavior.
